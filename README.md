# Swift 数组 Demo

## 简介

展示 Swift 数组的创建、访问、修改和常用操作。

## 启动和使用

```bash
cd swift-array-demo
swift run
```

## 教程

### 基本操作

- `append()`: 末尾添加
- `insert(at:)`: 指定位置插入
- `remove(at:)`: 删除指定位置元素

### 函数式操作

- `filter`: 过滤元素
- `map`: 转换元素
- `sort`: 排序
